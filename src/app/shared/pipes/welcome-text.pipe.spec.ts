import { WelcomeTextPipe } from './welcome-text.pipe';

describe('WelcomeTextPipe', () => {
  it('create an instance', () => {
    const pipe = new WelcomeTextPipe();
    expect(pipe).toBeTruthy();
  });
});
